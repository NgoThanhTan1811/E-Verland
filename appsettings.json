{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  
  "ConnectionStrings": {
    "UserDb": "",
    "CatalogDb": "Host=${DB_HOST};Port=${DB_PORT};Database=${CATALOG_DB};Username=${DB_USERNAME};Password=${DB_PASSWORD}",
    "ProductDb": "Host=${DB_HOST};Port=${DB_PORT};Database=${PRODUCT_DB};Username=${DB_USERNAME};Password=${DB_PASSWORD}",
    "AuthDb": "Host=${DB_HOST};Port=${DB_PORT};Database=${AUTH_DB};Username=${DB_USERNAME};Password=${DB_PASSWORD}",
    "PaymentDb": "Host=${DB_HOST};Port=${DB_PORT};Database=${PAYMENT_DB};Username=${DB_USERNAME};Password=${DB_PASSWORD}",
    "CheckoutDb": "Host=${DB_HOST};Port=${DB_PORT};Database=${CHECKOUT_DB};Username=${DB_USERNAME};Password=${DB_PASSWORD}",
    "OrderDb": "Host=${DB_HOST};Port=${DB_PORT};Database=${ORDER_DB};Username=${DB_USERNAME};Password=${DB_PASSWORD}",
    "CartDb": "Host=${DB_HOST};Port=${DB_PORT};Database=${CART_DB};Username=${DB_USERNAME};Password=${DB_PASSWORD}"
  }
}
